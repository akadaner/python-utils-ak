# necassary fields
microservice_name: example_heartbeat
configuration: default

broker: zmq
zmq:
    endpoints:
      heartbeat:
          type: sub
          endpoint: tcp://localhost:5555
      system:
        type: sub
        endpoint: tcp://localhost:5556