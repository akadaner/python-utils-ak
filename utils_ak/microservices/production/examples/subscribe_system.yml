# necassary fields
microservice_name: example_subscribe_system
configuration: default

zmq:
  endpoints:
    heartbeat:
      type: sub
      endpoint: tcp://localhost:5555
    system:
      type: pub
      endpoint: tcp://localhost:5556